# FloraSetu — Product/Architecture Requirements Document

## Original problem statement
Build 0 — Architecture Freeze (LOCKED): web-based B2B floriculture platform, mobile-first PWA, zero native code (Capacitor-ready, ADR-006). Implementation-grade architectural foundation only — no business functionality, no feature screens. Master Spec v2.0 is the single source of truth. Modular monolith, 12 strict bounded contexts, PostgreSQL + PostGIS authoritative store, Redis cache/locks/rate-limit, S3-compatible media via signed URLs, transactional outbox + queue (no Kafka). Cross-cutting: outbox, idempotency, optimistic concurrency, row locking, immutable audit, feature flags, effective-dated config, UTC, public ref IDs, webhook signature validation, trace-ID middleware, standard error envelope. ADR-001…006 resolved/owner-approved and baked into schema. Acceptance gate: build, typecheck, lint, migrations up/down, unit tests, tenant-isolation, audit-event, idempotency baselines — then STOP.

## User choices (Build 0 kickoff)
1. NestJS + TypeScript + PostgreSQL/PostGIS + Redis installed in-container (owner-approved deviation from env default FastAPI/MongoDB).
2. Monorepo: `/app/apps/api` + `/app/apps/web`; legacy template dirs untouched.
3. Docs-first deliverables as markdown under `/app/docs/` + root governance files.
4. Acceptance gate exactly as specified; no feature screens.

## User personas (future builds)
Org admin, buyer, supplier, QC agent, logistics ops, finance ops, support agent, read-only stakeholder. Build 0 models them as RBAC system roles only.

## Core requirements (static)
12 bounded contexts with enforced isolation; immutable historical/financial records; never-oversell inventory (ADR-001); cold-chain excursion HOLD gating with versioned handling profiles (ADR-002); post-settlement claims via adjustments/recoveries (ADR-003); payout/bank-change freeze + dual approval + irreversible submit (ADR-004); webhook dedupe + pre-transaction signature checks (ADR-005); mobile convertibility via bearer/OIDC + device abstractions (ADR-006).

## Implemented — 2026-06 (Build 0)
- Docs 01–11 (`/app/docs/`) + root governance: ARCHITECTURE_DECISIONS, OPEN_DECISIONS, DOMAIN_MODEL, REQUIREMENTS_TRACEABILITY_MATRIX, IMPLEMENTATION_STATUS, TEST_TRACEABILITY.
- Monorepo scaffold: NestJS API (:8001, `/api` prefix) + React/TS PWA shell (:3000) under supervisor (`florasetu-api`, `florasetu-web`).
- PostgreSQL 15 + PostGIS 3.3 + Redis 7 provisioned; 6 reversible migrations; 13 schemas, 68 tables incl. all later-phase tables (UI-inactive).
- Infra: error envelope, trace-ID middleware (AsyncLocalStorage), idempotency framework, immutable audit + security audit streams, transactional outbox + relay (SKIP LOCKED), feature flags, RBAC guard + org context, concurrency helpers, media stub signer, realtime/device interfaces, reference-ID generator, rate-limit config, health endpoint, dev-only baseline harness + seed.
- Tests: 4 architecture suites (boundary, no-cross-table, common-purity, no-cycles), 6 unit suites, 6 e2e suites — 15 suites / 35 tests, all green. Gate sequence all PASS.
- CI baseline workflow; infra bootstrap script (`scripts/bootstrap-infra.sh`); test credentials doc.

## Implemented — 2026-06 (Build 1, after Build 0)
- Identity & Party foundation: 17 org categories (exporter/government feature-gated), branches/addresses/contacts, multi-org memberships, permission catalog + 6 system roles seeded in migration 007.
- Auth: email/password (bcrypt), HMAC bearer access tokens (15 min) + rotating refresh tokens (7 d), TOTP MFA (enroll/verify, enforced at login), email-scoped Redis login lockout (5 fails → 15 min), `trust proxy` enabled.
- AuthZ: per-request DB-resolved org context (`X-Org-Id`), least privilege, 404-on-foreign object-level authz, suspension revokes transactional privileges (reads retained), platform privileged roles, audited time-boxed support access, role-assignment escalation allowlist.
- KYB: document submission + review workflow + immutable verification history.
- Bank/payout (ADR-004): immutable account history, masked reads, supplier-side only, PENDING_REVERIFICATION + payout freeze + dual approval (distinct approvers), dual audit streams. Settlement immutability verified against platform admin.
- UI: /login, /register, /onboarding, /account (profile, MFA, orgs, members, KYB, bank), /admin (masked org list, KYB review, suspend/lift, bank approvals).
- Owner account: nagesh.kgpl@gmail.com (PLATFORM_ADMIN, dev-seeded from env).
- Tests: build1-identity.e2e (12 gate tests) + all Build 0 suites — 17 suites / 56 tests green; migrations up/down (7) green; testing-agent verified incl. 4 bug fixes (ref-counter seed collision, ingress lockout bypass, admin X-Org-Id persistence, silent 500 logging).

## Implemented — 2026-06 (Build 2, after Builds 0+1)
- Catalog & Standards: canonical product model (categories with stable codes + optional hierarchy; commodities with botanical/common/commercial names; canonical aliases — Lisianthus/Eustoma; varieties with colour/form/stem-length; seasonality; launch city flags; product media metadata).
- Versioned effective-dated masters with overlap rejection: grade profiles (declarative JSONB rules vs attribute dictionary), pack definitions (nestable), product-scoped unit conversions (circular-chain/invalid rejection), handling profiles (range-validated).
- Defect taxonomy (11 classes), transport compatibility metadata (no blocking), supplier product capabilities (org-scoped), Postgres FTS + alias search returning canonical products only.
- Permissions: catalog.read / catalog.write / catalog.capability.write + CATALOG_MANAGER role. class-validator DTOs + global ValidationPipe (closes Build 1 DTO debt on shared infra).
- UI: /catalog, /catalog/admin, /catalog/capabilities (mobile 360/390/412). Seed: Phase-1 basket, DEMO-classified; units VALIDATED.
- ADR-007 (master-data versioning + canonical identity), OD-07/OD-08 (documented ambiguities).
- Tests: 18 suites / 76 tests green (build2 gate: 20 tests covering mandates 1–23; mandate 24 = agent viewport checks, PASS at 360/390/412 after nav/table-wrap fixes, iteration_6 final).

## Implemented — 2026-06 (Pre-Build-3 control gate)
- OD-07 RESOLVED — OWNER APPROVED: explicit `uom_id` at transaction boundaries; `preferred_order_uom_id` (commodity/pack) is UI preselection only; `POST /catalog/commercial-line/validate` (400 UOM_REQUIRED) + `POST /catalog/normalize-preview` (preserves original qty/uom, records conversionVersionId, normalized via ACTIVE versioned conversion only, mutates nothing).
- OD-08 RESOLVED — OWNER APPROVED: `validation_status` lifecycle (DEMO→PENDING_REVIEW→VALIDATED/REJECTED) separate from `status`, review metadata columns, separate-reviewer enforcement (403 SELF_APPROVAL) on grade/handling/conversion masters, `catalog.validate` + CATALOG_VALIDATOR role, production-use check ACTIVE+VALIDATED (+effective window), env escape `CATALOG_ALLOW_DEMO_MASTERS`. data_classification renamed → validation_status (migration 009).
- Migration 008 table count corrected: **11** new tables (Build 2 report typo).
- Lisianthus/Eustoma verified: single canonical commodity `PRD-SEED-LISIANTHUS`, alias `Eustoma` (BOTANICAL), zero duplicates, zero transactional references.
- Git baseline: commit `689543e`, tag `florasetu-build2-accepted`, `.env*` gitignored (no secrets committed; no remote push — awaiting owner authorization).
- Tests: 19 suites / 84 tests green (new prebuild3-gate suite: F1–F7 + rejection flow).

## Implemented — 2026-06 (Build 3, after Pre-Build-3 gate)
- Canonical demand model: events/ceremonies/BOM (user-defined), versioned requirements (QUICK/EVENT/FORMAL modes converge — no parallel model), RFQs + invitations, clarifications (visibility rules), immutable versioned quotations with OD-07 explicit UOM + OD-08 normalization metadata, awards with quantity invariants + deviation consent, ops procurement desk (procurement.manage).
- Migrations 010–011 (reversible; down-migration purge of Build-3 RFQ rows). Permissions + PROCUREMENT_OPS role seeded. Suspended orgs lose Build 3 transactional permissions.
- Guardrail A verified fail-closed (DEMO masters rejected; CATALOG_ALLOW_DEMO_MASTERS hard startup error in production; dev-only escape enabled in .env.development). Guardrail B: server-side master eligibility on every requirement line.
- Concurrency: one open RFQ per requirement (unique partial index), one quote per supplier per RFQ (23505→409), FOR UPDATE lifecycle transitions, award race invariants, idempotency on submit/publish/quote/revise/award.
- Cross-context DI via @Global contract modules (catalog/identity/notifications) — architecture boundary suites pass.
- Frontend: Quick Request (mobile-first), demand home, requirement workspace, events + BOM→requirement, RFQ comparison + award, supplier inbox + quote builder + clarifications, my quotes, ops desk. Permission-gated nav; stale-org login fix.
- Docs: docs/STATE_MACHINES.md, docs/SECURITY_MODEL.md created; RTM/DOMAIN_MODEL/API_INVENTORY/IMPLEMENTATION_STATUS updated.
- Tests: 20 suites / 146 tests green (build3-demand gate: 48 tests, groups A–I). Testing agent iteration_8: 11/11 frontend E2E flows PASS (desktop + 360/390/412 mobile).
- Git baseline: commit 6e1a8eb, tag florasetu-build3-accepted.

## Implemented — 2026-09-12 (Build 4, Pilot Fulfilment Core)
- 7 bounded-context modules: orders/allocations, supply lots, QC/custody, logistics/cold-chain, payments/settlements, claims, control tower.
- 11 React PWA screens; 74-test Build 4 gate incl. mandatory K1 end-to-end pilot simulation; full regression 196/196 across 11 suites; migrations reversible with pilot data; frontend verified (iterations 9–11).
- Hardened during Build 4: cross-org read isolation (procurement.manage-only ops), custody authz, media id==object_key contract, QC replay ordering, claims terminal-decision ledger, RBAC suspension coverage, lifecycle-aware settlement immutability (ADR-003 + Build-1 invariant merged).
- Fixed tenant-isolation bug where suppliers could see the buyer's order slice.
- Docs: docs/PILOT_OPERATING_FLOW.md, docs/BUILD4_ACCEPTANCE.md, API_INVENTORY.md Build 4 section.
- Incident 2026-09-15 (RESOLVED): pod restart wiped PG/Redis (data outside /app) and legacy template programs grabbed :8001/:3000 breaking login — recovery runbook in test_credentials.md (bootstrap-infra.sh → yarn seed → yarn seed:catalog → seed-pilot-order.sh, now dynamic-ID); login re-verified by testing agent iteration_12 (owner+supplier, wrong-password UX, 100%).

## Implemented — 2026-09-15 (UI/UX Forensic Audit & Redesign Specification — DOCS ONLY, NO UI CODE)
- Complete code-level audit of the Builds 0–4 React frontend (all 28 routes, shells, tokens, KYB/bank flows) against Master Spec v2.0.
- Deliverables (all in `/app/docs/`): UI_UX_FORENSIC_AUDIT.md, UI_UX_INFORMATION_ARCHITECTURE.md, UI_UX_DESIGN_SYSTEM.md, UI_UX_USER_JOURNEYS.md, UI_UX_RESPONSIVE_RULES.md, UI_UX_COMPONENT_INVENTORY.md, UI_UX_SCREEN_REDESIGN_PLAN.md, UI_UX_ACCEPTANCE_CRITERIA.md + machine-readable `/app/design_guidelines.json` (tokens, status vocabulary, 13 screen wireframes).
- Key findings: no role shells (one 18-link nav for all), home = internal architecture page, KYB flow functionally broken (hardcoded fake doc + hardcoded reject reason), org identity invisible at work time, raw backend status codes + UUID inputs as UI vocabulary, off-direction dark dev-console theme, no buyer decision surfaces/notifications, ops mechanics exposed to buyers, non-camera-first supply intake, non-field QC workbench (JSON textarea), role-mixed demand surfaces, minimal state coverage (loading/empty/403), mobile nav overflow.
- Redesign spec: 5 role shells (/buyer, /supplier, /partner, /ops, /admin) with role router at `/`; organization-first header + WorkspaceSwitcher; warm ivory light theme (#F8F8F5) + deep botanical green (#183D33) + Inter; StatusPill human labels; SearchableSelect kills UUID inputs; camera-first lot intake; full-screen QC field tool; exception-first ops board with SideSheet actions; KYB 5-step wizard + reviewer split view; responsive matrix 360→1920 with DataTable→MobileDataCard below 768px; WCAG 2.2 AA floor.
- Backend deltas identified (additive only, no behavior change): B1 structured KYB endpoints, B2 notifications feed, B3 supplier display name + verified flag in comparison payload. Zero modifications to accepted endpoint behavior; business logic/state machines/authorization/audit untouched.
- 8-phase build sequence defined (tokens/components → shells → buyer → supplier → partner QC → ops → admin → responsive/AA hardening). Acceptance criteria testable per phase with standard test accounts.
- OWNER DECISION (2026-09): audit summary delivered; WAITING for explicit owner approval before ANY React redesign coding. Build order on approval: follow REDESIGN_PLAN §7 doc order (phases 1–8).

## Implemented — 2026-09-16 (UI/UX Redesign Phase 5: Independent Logistics Partner + Driver Workspace — owner directive, ADR-012)
- Owner ruling enforced in backend: FloraSetu never operates logistics. Staff cannot assign partner drivers/vehicles (legacy ops assign endpoint is partner-org-only; driverUserId → 403 DRIVER_ASSIGNMENT_PARTNER_ONLY) nor record partner execution milestones. Partner managers (new `logistics.execute` / `logistics.assign_driver` permissions, ORG_ADMIN-seeded) and assigned drivers execute; drivers are job-scoped (404-on-foreign on unrelated jobs).
- Migration 015 (additive; up/down gate-verified): append-only immutable `logistics.execution_events` (seq-ordered; one-time-milestone unique guard collapses retries), `logistics.driver_assignments` history, shipments +vehicle_ref/+handling_note/+arrived_pickup_at/+arrived_delivery_at, pod_records +pod_ref/+signature_media_object_id.
- Execution lifecycle on existing endpoints + new arrived-pickup/arrived-delivery: guarded transitions (accept → arrived pickup → pickup → transit → arrived delivery → deliver+POD), idempotent replays, events written in-transaction with immutable audit.
- Partner-controlled driver assignment: same-org ACTIVE members only via identity contract (`isActiveMember`/`listActiveMembers`/`getUserDisplayNames` added; ADR-010 unchanged); assign/reassign/unassign preserve full history. New endpoints: `jobs/dashboard`, `jobs/eligible-drivers`, `jobs/:id/assign-driver|unassign-driver|arrived-pickup|arrived-delivery`.
- POD = structured evidence (photo + optional signature file + documentary reference); never implies quality acceptance, claim closure or payment release. Partner exceptions remain non-blocking (ADR-002/011 intact). Multimodal: bus/rail/air driverless with operator/terminal/AWB references; road modes carry optional driver + vehicle.
- Web (LOGISTICS-only partner shell): PartnerHomePage (7 attention buckets; driver variant = Today's jobs), PartnerJobsPage (bucket + mode filters, human mode labels, driver scoping), PartnerJobDetailPage (pickup/delivery/transport cards, multimodal panels, member-picker assign, execution stepper, POD form, exceptions, event timeline); nav Home/Jobs/Delivered. Zero QC/enum/UUID leakage.
- Seed: `scripts/seed-phase5-logistics.sh` (8 tagged scenarios across all modes on Nilgiri Fresh Logistics; API-readiness wait) + demo.driver2 persona.
- Gates: backend 24 suites / 256 tests green (new phase5-logistics-partner e2e = 17 tests L1–L17; prephase3 L1 updated to ADR-012 flow; zz-migrations 14→15); typecheck + lint + web build green; testing agent iteration_18 frontend 100% PASS (1440 desktop, 390 mobile, all acceptance flows incl. assign/reassign/unassign, full POD chain, driver isolation, terminology scan).

## Implemented — 2026-09-16 (UI/UX Redesign Phase 6: Operations Control Tower & Discipline Workspaces — owner directive, ADR-013)
- Exception-first `/ops/exceptions` board: server-composed lanes (PROCUREMENT/ORDERS/LOGISTICS/CLAIMS/FINANCE) with per-caller `allowedActions` (frontend renders, never decides), lane filters, scoped staff search, audited CSV export (`tower.board_export`; fixed uuid-typed `object_id` audit bug found by phase6 S6).
- Discipline workspaces: `/ops/orders` + `/ops/logistics` monitors (SideSheet detail, human next-action owners, SLA/delay/POD flags — logistics strictly read-only per ADR-012, zero execution controls), `/ops/claims` + `/ops/claims/:id` (evidence-first; decision panel renders only for `claim.manage` — support is read-only per owner ruling, no invented triage mutations), `/ops/finance` (verification queues, record payment/settlement forms, ADR-004 payout-freeze visibility from the existing audited bank-change workflow, recorder≠verifier §29 preserved, no unilateral freeze/unfreeze).
- OpsShell nav role-filtered (Exceptions all; Procurement PROCUREMENT_OPS; Orders all ops; Logistics PROCUREMENT_OPS+SUPPORT_AGENT; Claims all ops; Finance FINANCE_OPS); Quality/Sourcing removed (ADR-011), legacy paths redirected. Legacy buyer Claims pages retained on buyer routes only.
- Backend authorization gate `test/e2e/phase6-ops-control-tower.e2e-spec.ts` (13 tests S1–S6): tower.read gating, lane isolation, support cannot transition claims (403), procurement/support cannot verify/record finance (403), all ops roles locked out of partner driver assignment/milestones/POD (ADR-012 reconfirmed), audited export.
- Seed fix: `seed-phase2-personas.ts` assigned SUPPORT_AGENT role to demo.support without the org membership (testing agent iteration_19 blocker) — fixed in seed + live dev DB.
- Gates: FULL backend 25 suites / 269 tests green (baseline was 256 pre-Phase-6; +13 new), zz-migrations expected count 15→16, web typecheck/lint/build green, testing agent iteration_19 (9/10 PASS; support flow re-verified by main agent after seed fix: nav isolation, claims 200, transition 403).

## Implemented — 2026-09-16 (UI/UX Redesign Phase 7: Admin Control Plane — owner directive, ADR-014)
- Admin Control Plane `/admin/*` (11 pages): Overview (governance metrics + recent admin activity), Organizations (search/type/status/KYB filters) + Org detail (identity, capabilities, members, verification history, reason-gated suspend/reactivate, per-org audit), KYB queue + evidence-first reviewer (signed document URLs, Approve / Request Correction / Reject with structured reason codes; correction = REJECTED+CORRECTION_REQUIRED+mandatory note, renders "Correction requested"; state machine unchanged), Users & access (search/status filters, reason-gated suspend/reactivate, self-suspend blocked in UI+backend, login+org-context blocked for suspended users), Roles read-only matrix (no IAM designer), Configuration (existing keys only, old/new+actor+reason audit), Feature flags (effective-dated, confirm+reason, never bypass RBAC), Security (privileged-user MFA state, break-glass grants, security events), Audit logs (action/object/trace/date filters, authorized CSV export, immutable).
- Backend: migration 017 (admin.user.read/admin.user.manage/flag.manage → PLATFORM_ADMIN; verification_history.reason_code); admin endpoints (overview, kyb-queue, kyb detail with signed URLs, users list/suspend/reactivate, roles matrix, security overview, extended audit search/export); AdminConfigController (config read/write + flag toggle, audited); KYB review hardened (structured reason mandatory on REJECTED); OrgContextService rejects non-ACTIVE users; members list via assertOrgAccessOrPlatform.
- Fixes from testing (iteration_20): API client 204/empty-body guard (spurious error alerts on all void admin actions), users search submit button, self-suspend UI guard, per-org audit filter by object_id.
- Gates: FULL backend 26 suites / 300 tests green (269 baseline + 26 phase7 + 5… exact: 300 total), zz-migrations 16→17, web typecheck/lint/build green, testing agent iteration_20 (14/15 PASS; all 5 issues fixed + self-verified: suspend/reactivate notices, org audit populated, self-guard, search).

## Implemented — 2026-09-16 (UI/UX Redesign Phase 8 FINAL: Hardening & Pilot Release Candidate — owner directive, feature freeze)
- Legacy chrome eliminated: all 8 remaining pre-Phase-1 pages rebuilt on the design system (buyer Issues list/detail, Account & org with REAL KYB file upload — fake 1KB document removed, Catalog with human validation labels, supplier Capabilities with product→variety pickers replacing raw UUID input, buyer Requirement workspace — raw supplier-UUID field removed + deadline min-constraint + UTC conversion, Ops procurement desk, Admin catalog with human version summaries replacing raw JSON). 7 confirmed dead pages deleted after zero-reference trace.
- PWA: service worker (public/sw.js) — cache-first only hashed /assets|/icons|/brand, network-first navigations with offline shell fallback, /api NEVER cached (no tokens/KYB/bank/claim/POD/signed URLs), versioned caches with activation cleanup, skipWaiting + controllerchange reload for version upgrades; registration enabled on preview (dev-safe policy) and prod.
- Security: media upload allowlist (JPEG/PNG/WebP/PDF/MP4) + magic-byte content-signature verification (declared Content-Type never trusted), 15MB cap with clean 4xx (express json 30mb body limit in main.ts + test helpers); SecurityHeadersMiddleware (module-registered, applies to prod + test apps): nosniff, X-Frame-Options DENY, Referrer-Policy, Permissions-Policy, CSP Report-Only baseline.
- Navigation: wizard "Request sent" → "Track this request" link + BuyerHome recent requests → "Track" links (orphan /buyer/requests/:id closed); BottomNav container testids; retired-route redirects verified live.
- Gates: FULL backend 27 suites / 307 tests green (incl. NEW phase8-hardening spec: upload security U1-U5, headers H1, error contract H2), web typecheck/lint/build green, testing agent iterations 21+22 (all UAT items GREEN or documented PARTIAL: offline shell fully effective only on production bundles — Vite-dev preview limitation, not a defect).
- No new ADR: Phase 8 changed no architecture decisions; ADR-010…014 verified intact. Native datetime-local retained per owner ruling (mobile-native, accessible; UTC conversion verified).

## Implemented — 2026-09-16 (Public Website & Brand Polish — owner directive, post-Phase-8)
- Logo defect corrected: petals previously crossed below the bridge arc (bases at y=44 vs arc apex y=30); redrawn so the flower rises from the arc apex with clean negative space — one geometry across Logo.tsx, all 7 brand SVGs, favicon, manifest icon. Wordmark: Fraunces 600 two-tone (Flora #183D33 green / Setu #76518F plum), header mark 44px / wordmark ~28px (80px header).
- Public chrome extracted (components/PublicChrome.tsx): premium sticky header (logo always links /), dark botanical footer with organized columns + REAL links (Privacy Policy → /privacy, Terms of Use → /terms — placeholders removed).
- New public pages: /privacy + /terms (pilot-scope honest drafts — NEEDS LEGAL REVIEW before production), shared readable legal layout, unique titles. SEO: title/meta description/canonical/OG/Twitter/Organization+WebSite JSON-LD.
- Auth usability: AuthLayout (logo→/, ← Back to home) on Login/Register/Forgot/Reset; Login gains "Forgot password?"; Register keeps "Already have an account? Sign in".
- Password recovery (NEW backend, migration 018): POST /auth/forgot-password (neutral anti-enumeration, per-email rate limit, single-use 60-min SHA-256-hashed tokens, non-production devResetToken channel — no email provider in pilot) + POST /auth/reset-password (server-side policy, one-time use, all refresh tokens revoked on reset). e2e: password-reset.e2e-spec.ts (P5-P13).
- Hero images: explicit width/height + srcset/sizes (CLS-safe), eager LCP hero, lazy below-fold.
- Gates: FULL backend 28 suites / 313 tests green (zz-migrations 17→18), web typecheck/lint/build green, testing agent iteration_23 (acceptance IDs BRAND-B*, PUBLIC-P*, AUTH-N*, AUTH-P*, IMAGE-I*).

## Implemented — 2026-09-16 (FINAL Brand Identity Verification — owner re-directive, iteration 24)
- ROOT CAUSE of "wordmark renders as generic sans" found + fixed: typography utility `:is(.fs-shell, .fs-preview, [class^='fs-'], [class*=' fs-']) { font-family: var(--fs-font) }` in tokens.css was directly targeting the `.fs-logo__flora`/`.fs-logo__setu` child spans (class starts with `fs-`), defeating inheritance from the Fraunces wordmark rule. Fix: `font-family: inherit` on both spans in components.css (equal specificity, imported after tokens.css → wins).
- testing_agent iteration_24: 7/7 BRAND-F acceptance GREEN with computed-style proofs — Desktop 1440px: Fraunces Variable 600 stack on both spans, wordmark 33.6px (target 32–36), mark 50×50 (target 46–52), Flora rgb(24,61,51)=#183D33 / Setu rgb(118,81,143)=#76518F; Mobile 390px: mark 40×40 (target 36–42), wordmark 24.8px (target 24–28); Tablet 768px no overflow; geometry verified (petals originate at arc crown (32,34), all control points y≤34 — nothing below the arc); auth pages (/login, /register, /forgot-password) Fraunces + logo→/ + back-to-home; footer light variant Fraunces ivory/plum-tint, legible; nav/CTA regression clean.
- Cosmetic: React 18 `fetchPriority` JSX warning on hero <img> silenced via lowercase spread attribute.

## Implemented — 2026-09-16 (Brand Board Redesign — owner directive with reference mockups, iteration 25)
- Owner supplied brand identity board + homepage reference mockups and ordered the public homepage + logo to match. Owner rulings: KEEP two-tone wordmark (Flora green / Setu plum) with the reference's serif style; SKIP the stats row entirely; FULL homepage restyle; existing copy unchanged; roses hero replaced with elegant white lilies.
- New brand palette (brand board): Primary #0F3D2E, hover #0B2F24, tint #E9F0EB, Accent Plum #6F4B8B, tint #F4EEF7, Surface #FCFBF6, Border #E2E2DD, Text #1F2937 — applied to public scope (.fs-pub vars) + logo lockup (components.css wordmark colors hardcoded).
- New logo mark geometry (Logo.tsx + all 7 brand SVGs + favicon): detached arc `M7 54 A25 20 0 0 1 57 54` (stroke 7), broader leaf side petals, central plum teardrop; petal junction (32,31) sits just over the arc crown — nothing crosses below. Light variants #F8F8F5/#D9C2E3.
- PublicHomePage restyle (copy + data-testids preserved): Fraunces Variable as public display font (--fs-font-brand override in .fs-pub); 3-col hero (content | white-lily image | stacked italic serif "People Flowers Progress Together"); white trust strip with circular green-outlined lucide icons; how-it-works plum number circles with connector lines; solution cards with photo tops; feature cards with plum-tint icon chips; #about converted to deep-green org band with check icons + white-lilies-on-dark image; Sign in restyled as text link; responsive at 900/520px.
- Gates: web typecheck + build green; testing agent iteration_25 — 11/11 redesign acceptance items (R1–R11) PASS at 1440/768/390: geometry/colors/fonts computed-verified, no stats strings, no overflow, no console errors on /, /login, /register, /privacy, /terms, mobile hamburger OK.

## Implemented — 2026-09-16 (Self-hosted marketing imagery + borderless blend — owner directive)
- All 8 marketing images moved off Pexels/Unsplash CDNs into the app: `/app/apps/web/public/images/*.jpg` (hero-lilies, band-lilies-dark, solutions-buyers/suppliers/logistics, event-flowers, supplier-facility, lot-evidence; ~1.8MB total, verified valid JPEGs, served 200 by the web server). PublicHomePage now references local paths only — zero third-party image dependency.
- Borderless image treatment per owner correction: removed all border-radius/box-shadow/card frames from big imagery; edges now feather into the page background via CSS masks — hero + split images use soft radial vignettes (ellipse ~98%, 48→92% fade), the dark org-band image uses an intersected linear mask (24% left fade into the green band + 10% top/bottom fades). Card photos inside bordered solution cards intentionally unchanged (they are cards by design).
- Verified: typecheck + build green; pixel-level edge crops confirm masks render; mobile 390px no overflow.

## Status
BUILD 0: PASS. BUILD 1: PASS. BUILD 2: PASS. PRE-BUILD-3 GATE: PASS. BUILD 3: PASS. BUILD 4 (Pilot Fulfilment Core): PASS — 2026-09-12 (196/196 e2e green). UI/UX FORENSIC AUDIT: COMPLETE — 2026-09-15 (docs only, no code). UI/UX REDESIGN PHASE 4: IMPLEMENTED + TESTED — 2026-09-16 (owner-authorized). Supplier workspace: SupplierHome attention-first (new requests, offers due ≤48h, orders to fulfil, supply needing evidence, dispatches pending, payout status; primary VIEW REQUESTS + ADD SUPPLY); SupplierInboxPage request cards (no UUIDs, deadline-sorted); SupplierRfqPage rebuilt as request summary (flower/spec/qty/UoM/needed/destination/deadline) + quote builder (qty, UoM, unit price, freight INCLUDED/EXTRA/PENDING with amount, deviation, delivery commitment, validity, notes, LIVE TOTAL, revision with mandatory reason) + Decline (reason-gated) + Ask buyer (clarifications); AddSupplyPage camera-first 7-step wizard (photos→flower→qty+UoM→batch & declared spec→harvest/received→optional video→review→submit evidence; min-2-photos gate; fs-desktop-only + sticky mobile CTAs; success screen); LotDetailPage rebuilt (evidence completeness checklist, photo/video gallery, declaration card, balances grid, orders linked via reservations, custody timeline, hold resolution — no 'QC approved'); SupplierOrdersPage cards + new SupplierOrderDetailPage fulfilment flow (confirm order → lot linked → evidence → pack form → logistics handoff showing partner name via new logistics_org_name on listForOrder → delivered); PayoutsPage (gross/adjustments/claim adjustment/net/status/UTR). Fixes from testing: originType enum mapping (OWN_FARM/MARKET_PURCHASE — CRITICAL blocker), stale freight amount cleared on toggle. Backend additive: shipments.listForOrder enriched with logistics_org_name via identity contract (e2e asserted in L1). Gates: backend 13 suites/215 tests green, web build/lint green, testing agent iteration_17 (10/11 PASS, 1 blocker fixed + self-verified end-to-end: wizard submit → lot AVAILABLE with declaration+completeness; fresh order ORD-2026-000002 confirm→allocate→pack via UI with steps/logistics verified at 1440px). UI/UX REDESIGN PHASE 3: IMPLEMENTED + TESTED — 2026-09-16. Buyer Experience (BuyerHome attention feed, GetFlowersPage 60-second flow with OD-07 UoM, OfferComparisonPage with freight/landed-cost/sorting/consequence-first award, BuyerOrderPage 9-step timeline + evidence pack + delivery acceptance + evidence-first claims, OrdersPage/EventsPage/EventDetailPage event-first, Buy Again ?copy=) + Public brand homepage at / for logged-out users (hero/trust strip/how-it-works/audiences/features/org-first/event/supplier/evidence/final CTA/footer, no fake stats, no QC claims). Backend additive: B3 comparison supplier name+KYB (identity getOrgPublicProfiles contract), GET /orders/:id/evidence-pack (supply getLotEvidence + logistics getShipmentsEvidenceForOrder/getPackEvidenceForOrder contracts, signed URLs), ParseUUIDPipe hardening, acceptDelivery allows CLAIM_OPEN, supplier capability seed for managed QUICK matching. Testing agent iterations 15+16; fixes verified: desktop CTA visibility, public button contrast, draft persistence, RFQ inbox endpoint correction + capability seed, timeline double-format Invalid Date. PRE-PHASE-3 PRODUCT CORRECTION GATE (ADR-011): PASS — 2026-09-15 (owner accepted): migration 014 (supplier declaration + lot evidence + logistics shipments/POD/exceptions + PARTNER_LOGISTICS capability), partner shell QC→logistics pivot, supplier declaration UI, ADR-011 documented. UI/UX REDESIGN PHASE 2: IMPLEMENTED — 2026-09-15: root workspace router; five lazy shells; WorkspaceGuard; WorkspaceSwitcher; NotificationBell; role-filtered ops nav; legacy redirects; draft isolation; migration 013; testing agent iteration_13 28/28 PASS. UI/UX REDESIGN PHASE 1: IMPLEMENTED — 2026-09-15: owner rulings UX-ADR-001…008; design tokens; 19 foundational components; ShellFrame primitives; status vocabulary; dev-only /dev/design-system preview. Tech debt: stub media signer, dev HMAC token format (pending OD-02 OIDC), KMS for TOTP secrets, audit partitioning, native datetime-local inputs (design-system calendar later), /auth/login 201-vs-200 nit. PILOT PRODUCTION BLOCKERS: OD-02 (production auth/TOTP) and OD-03 (durable media storage) — external pilot blocked on both; internal/dev pilot accepted. UI/UX REDESIGN PHASE 4: ACCEPTED (owner issued Phase 5 directive). UI/UX REDESIGN PHASE 5: ACCEPTED (owner issued Phase 6 directive). UI/UX REDESIGN PHASE 6: ACCEPTED (owner issued Phase 7 directive). UI/UX REDESIGN PHASE 7: ACCEPTED (owner issued Phase 8 FINAL directive). UI/UX REDESIGN PHASE 8: ACCEPTED (owner issued public-website/brand polish directive). PUBLIC WEBSITE & BRAND POLISH: IMPLEMENTED + TESTED — 2026-09-16 (see Public Website section above; 28 suites/313 tests green; testing agent iteration_23: 24/24 acceptance IDs GREEN incl. BRAND-B1-B5 geometry/colour/font proofs, PUBLIC-P1-P3, AUTH-N1-N5, AUTH-P1-P15, IMAGE-I1-I2; P3 390px overflow fixed post-run). NEXT: owner acceptance → pilot go-live checklist (legal review of /privacy+/terms, production email provider for reset links, OD-02 OIDC, OD-03 durable media storage).

## Prioritized backlog
- P0: UI/UX redesign Phases 2–8 (Phase 1 committed on `uiux/pilot-redesign`, awaiting owner visual acceptance). Phase 2 = role/workspace router + 5 shells + WorkspaceSwitcher + notification placeholder; then buyer, supplier, partner QC, ops, admin, responsive/AA hardening; backend deltas B1/B2/B3 only in their phases (UX-ADR-007).
- P0 (Build 4 architecture regression — RESOLVED 2026-09): `no-cross-table` fixed correctly (regex lookbehind distinguishes raw `schema.table` SQL from allowed contract property access; custody.service.ts rerouted via SupplyInventory contract). `no-cycles` RESOLVED BY OWNER-APPROVED **ADR-010**: two authorized synchronous contract pairs (order-allocation↔supply-inventory for reservation/ownership invariants; order-allocation↔logistics-coldchain for the ADR-002 `hasBlockingException` acceptance guard) — explicit test allowlist with contracts-only evidence check; all other cycles still FAIL. Also fixed pre-existing float-literal lint violation (celsius 6.5→7) in build4 e2e spec. FULL GATE GREEN: 21 suites / 220 tests (architecture+unit+e2e), typecheck, lint, build. Phase 2 unblocked pending owner go.
- P1: OIDC provider selection (OD-02) to replace dev HMAC tokens; S3 endpoint + real media signing (OD-03); payments provider (OD-01); notification delivery (OD-04/05); partner hub/logistics shells (phase 2, needs B4 trip-assignment endpoint).
- P2: live auctions, analytics pipeline, Capacitor wrap, audit partitioning, dark mode for night-hub logistics.

## Next tasks
1. OWNER ACCEPTANCE of Phase 8 (hardening + UAT) → label the build FLORASETU PILOT RELEASE CANDIDATE.
2. Pre-live-pilot blockers (already documented): OD-02 production OIDC, OD-03 durable object storage (media is local-disk), CSP Report-Only → enforcing after violation review, offline-shell validation on a production bundle, production seed isolation (demo accounts must never auto-seed in production).
3. Post-pilot deferred register: directed-RFQ supplier picker, inline KYB document viewer pane, design-system calendar (native datetime-local retained by owner ruling), background offline-mutation replay for field flows.
